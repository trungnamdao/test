import * as THREE from 'three';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js';
import fs from 'fs';

// To load GLB in node, we'd need some polyfills, maybe it's easier to just run a quick server and log it to browser console, or just assume the materials have colors.
